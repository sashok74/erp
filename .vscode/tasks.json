{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "cargo build gateway",
      "type": "shell",
      "command": "cargo build -p gateway",
      "group": "build",
      "problemMatcher": ["$rustc"]
    }
  ]
}
