#![warn(clippy::pedantic)]
#![allow(clippy::module_name_repetitions)]

//! ERP Warehouse — MVP domain: складской учёт.
//! Domain (aggregates, events), Application (commands, queries), Infrastructure (pg repo).
